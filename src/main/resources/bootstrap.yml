spring:
  application:
    name: activities-config-microservice
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: https://github.com/mahanhz/activities-config
          searchPaths: config*
